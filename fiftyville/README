This project includes the database (which is not viewable through github but you can view the schema in schema.txt), a log in which I recorderd all of my queries and the reasoning for each query, and an answers.txt file
  in which I recorded the answers for who committed the robbery, who the accomplice was, and where the thief fled to afterward.
