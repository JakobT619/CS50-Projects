This project includes the database (which is not viewable through github but you can view the schema in schema.txt) and a log in which I recorderd all of my queries and the reasoning for each query.
The objective was to write a single SQL query that outputs the desired results for each problem, which is included with each sql file.
